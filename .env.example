# Required for any custom topic: Claude builds step-by-step chalk blueprints. Optional short phrases may still use local fixtures without a key.
ANTHROPIC_API_KEY=
# Google Identity Services (Sign in with Google): Web client ID from Google Cloud Console
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
# Set to 1 to skip the API and use only local fixtures (offline / no credits)
VISUA_AI_USE_FIXTURES_ONLY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# ElevenLabs TTS for narration on the chalkboard. Server-only (no NEXT_PUBLIC_ prefix).
ELEVENLABS_API_KEY=
